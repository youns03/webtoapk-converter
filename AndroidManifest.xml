<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.webtopack"> <!-- This will be updated by the Python script -->

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher" <!-- This will be updated by the Python script -->
        android:label="@string/app_name" <!-- This will be updated by the Python script -->
        android:roundIcon="@mipmap/ic_launcher_round" <!-- This will be updated by the Python script -->
        android:supportsRtl="true"
        android:theme="@style/Theme.WebToAPK"
        android:hardwareAccelerated="true">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|keyboardHidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
